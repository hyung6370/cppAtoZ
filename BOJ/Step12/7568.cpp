#include <iostream>
using namespace std;

int main(int argc, char* argv[]) { 
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int num;
    cin >> num;

    for (int i = 0; i < num; i++) {
        int kg;
        int height;
        cin >> kg >> height;

        
    }

    return 0;
}