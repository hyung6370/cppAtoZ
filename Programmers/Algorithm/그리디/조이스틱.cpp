#include <iostream>
#include <vector>
#include <string>
using namespace std;

int solution(string name) {
  int answer = 0;

  

  return answer;
}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);

  string name;
  cin >> name;

  cout << solution(name);

  return 0;
}