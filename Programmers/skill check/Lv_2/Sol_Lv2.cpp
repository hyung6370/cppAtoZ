#include <iostream>
#include <string>
#include <vector>
using namespace std;

vector<int> solution(vector<int> fees, vector<string> records) {
    vector<int> answer;
    return answer;
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);


    return 0;
}