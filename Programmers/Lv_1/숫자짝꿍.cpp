#include <iostream>
#include <string>
#include <vector>
using namespace std;

string solution(string X, string Y) {
    string answer = "";



    return answer;
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    string X, Y;
    cin >> X >> Y;

    cout << solution(X, Y);

    return 0;
}